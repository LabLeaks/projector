# Trigger Examples

Use this skill when the request is about architecture correspondence, for example:

- "Review whether this module is honestly implemented."
- "Use `special modules PROJECTOR.RUNTIME.DAEMON --verbose` and tell me if the implementation matches the intent."
- "Use `special modules PROJECTOR.SERVER.STORAGE --metrics --verbose` and tell me what looks suspicious."
- "Do these `@fileimplements` tags look hand-wavy?"
