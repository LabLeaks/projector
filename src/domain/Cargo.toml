[package]
name = "projector-domain"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lib]
name = "projector_domain"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
